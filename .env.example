OPENAI_API_KEY="sk-..."
GOOGLE_CREDENTIALS_PATH="./service_account.json"
GOOGLE_SHEET_URL="https://docs.google.com/spreadsheets/d/..."

# Groq API (이슈 #156: ai_engine.py AI 호출 antigravity → Groq 전환)
# 무료로 발급 가능: https://console.groq.com/keys
GROQ_API_KEY="gsk_..."
# 선택 항목 — 비워두면 코드 기본값(qwen/qwen3.6-27b) 사용. Groq 라인업이 바뀌면
# https://console.groq.com/docs/models 에서 다시 확인 후 여기서 덮어쓸 것.
# GROQ_MODEL="qwen/qwen3.6-27b"
# 선택 항목 — qwen3.6-27b는 기본이 thinking 모드라 매 호출마다 긴 추론 텍스트를
# 먼저 출력한다. 단순 분류 작업엔 불필요해 코드 기본값이 "none"(꺼짐)으로 되어 있음.
# 모델을 바꿔서 이 파라미터가 오류를 내면 빈 문자열로 설정해 아예 안 보내게 할 수 있음.
# GROQ_REASONING_EFFORT="none"

# --- 대시보드 "데이터 갱신" 버튼 (이슈 #152, #164, #165) ---
# 트리거 서버(관리자가 별도 인프라에서 운영, 이슈 #164)의 주소.
# ⚠️ src/web은 output: "export"로 정적 익스포트되므로, NEXT_PUBLIC_ 로 시작하는
# 값은 빌드 시점에 JS 번들에 그대로 박혀 브라우저에서 볼 수 있게 됨.
# 아래 두 값은 완전히 비밀로 유지되지 않는다는 걸 전제로 설정할 것
# (스팸성 재실행을 막는 최소한의 장치 수준). 계약 상세는 이슈 #164 참고.
NEXT_PUBLIC_PIPELINE_TRIGGER_URL="https://pipeline.sonagi.space"
NEXT_PUBLIC_PIPELINE_TRIGGER_TOKEN="change-me"
