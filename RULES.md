# WorkExperience_1953 (일경험 프로젝트) 지식 베이스 및 RULES

> 본 문서는 로컬웨이브 팀의 '1953형제돼지국밥 온라인 반응 수집·분석 자동화 프로젝트'를 위한 **단일 진실 공급원(Single Source of Truth)** 입니다.
> 팀원뿐만 아니라 OpenCode Agent(AI)가 코드를 작성하고 데이터를 정제할 때 최우선으로 준수해야 하는 절대 규칙을 담고 있습니다.

---

## 1. 프로젝트 및 환경 표준 (Project & Environment Standards)

### 1.1 디렉토리 구조
파편화된 기존 로컬 자료를 정리하고, 파이프라인의 실행을 자동화하기 위해 다음과 같은 구조를 강제합니다.
*   `docs/`: 추출된 Notion 문서, 구조화된 기획서, 회의록 등
*   `docs/design/`: 디자인 프로토타입 원본. UI 시각 규격의 기준 (3.2.1 참고)
*   `data/raw/`: 원천 데이터 (API 수집본 등)
*   `data/clean/`: 정제가 완료된 분석 대상 데이터
*   `src/pipeline/`: [Python] 데이터 수집 및 정제 스크립트
*   `src/web/`: [Frontend] 대시보드 웹 애플리케이션

### 1.2 실행 환경 및 결과물 공유
*   **패키지 관리:** 파이썬 스크립트 실행에 필요한 모든 의존성은 `src/pipeline/requirements.txt`에 명시하고 가상환경을 통해 격리합니다.
*   **데이터 종착지:** 정제 및 분석(감성/테마 분류)이 완료된 최종 결과물은 팀원 공유를 위해 **Google Sheets**로 자동 업로드되어야 합니다.

---

## 2. 데이터 수집 및 정제 기준 (Data Processing Rules)

### 2.1 플랫폼별 수집 기준 (Collection Matrix)
> **(2026-07-29 개정, 멘토 사전 협의 완료)** 채널 제한을 완화했습니다. 개인정보 배제 원칙은 그대로 유지됩니다.
*   **허용 채널:** 오너 콘솔(네이버 스마트플레이스, 배민/요기요 사장님 계정, 구글 GBP), 공식 오픈 API(네이버 검색 API, YouTube Data API)에 더해 구글맵·캐치테이블 등 공개된 리뷰 페이지의 수동/반자동 열람(OpenDevBrowser 등)도 허용합니다.
*   **비공개(문서화되지 않은) API 재사용 — 조건부 허용:** 인증서 고정(certificate pinning) 등 기술적 보호조치를 무력화하지 않고, 관찰 가능한 범위(예: 일반 TLS 트래픽을 프록시로 관찰)에서 앱이 자체적으로 호출하는 비공개 API 엔드포인트를 찾아 재사용하는 것은 허용합니다. 모바일 앱(캐치테이블·배달의민족 등)에 적용 전, 해당 앱이 인증서 고정을 쓰는지 먼저 확인합니다 — 안 쓰면 이 조항 적용, 쓰면 아래 금지 항목에 해당합니다.
*   **여전히 금지:** 로그인 필요 구간의 접근제어 우회 수집(비공개·회원전용 게시물 등), 인증서 고정 등 기술적 보호조치를 직접 무력화(우회)하는 행위. 수집 시 개인 식별정보(실명, 얼굴 사진, 연락처 등)는 배제/마스킹합니다.
*   **지속(파이프라인) 수집 전환 검토:** 구글맵/캐치테이블 리뷰를 일회성이 아닌 정기 수집으로 편입할지에 대한 옵션·비용·리스크 비교는 `docs/collection-pipeline-feasibility.md` 참고. 유료 스크래핑 API 사용 등 비용이 드는 결정은 사용자 확인 후 진행합니다.

### 2.2 핵심 정제 규칙 (Clean Rules)
수집된 데이터(언급) 중 분석 대상만 남기기 위해 다음 규칙(`R1~R4`)을 적용합니다.

*   **R1 (협찬 배제):** 제목이나 본문에 다음 키워드가 없어야 함. (`협찬, 제공받, 원고료, 체험단, 서포터즈, 소정의, 증정`)
*   **R2 (기간 필터):** 폐지함 (2026-07-29 개정, 멘토 사전 협의 완료). 기간 제한 없이 수집 가능합니다. 다만 작성일이 결측이거나 상대 표현("3주 전" 등)만 존재하는 경우, 정규화(근사 연-월 변환 등) 전까지는 월별 추이 분석에서 제외합니다.
*   **R3 (유튜브 1953 관련성):** `1953관련 == 'Y'` 인 영상의 댓글만 유지.
*   **R4 (유튜브 유효성):** 댓글 내용이 존재하는 행만 유지. (영상 메타 정보 행 제외)

### 2.3 정제 검증 기준 (Validation)
> **(2026-08-19 갱신)** 아래는 과거(R2 기간 필터 적용 시절) 기준이었으나, R2 폐지(§2.2) 이후 이 고정 수치와의 일치를 강제하는 검증(`exit code 1`)은 **`clean_mentions.py`에서 이미 제거되었습니다.** R2를 없애면 오래된 언급이 더 포함돼 건수가 늘어나는 게 정상이고, 구글맱·캐치테이블 등 신규 채널까지 추가되는 이상 총 건수는 계속 달라집니다. 지금은 실행할 때마다 채널별 실제 건수를 화면에 출력만 하고(과거 기준값은 참고용으로 나란히 표시), 강제 실패시키지 않습니다.

*   과거(R2 적용 시절, 네이버·유튜브만) 기준값 — 참고용: 네이버 카페 1,193건 | 네이버 블로그 486건 | 네이버 뉴스 7건 | 유튜브 댓글 30건 | 총합 1,716건
*   지금 채널 구성(네이버 + 유튜브 + 네이버 플레이스 영수증 + 카카오맵)에서는 이 값과 달라지는 것이 정상이며, 고정된 "맞춰야 할 목표 총량"은 더 이상 존재하지 않습니다.
*   엄격 검증을 다시 켜고 싶다면(예: CI에서 이상 급증/급감 탐지 등), `src/pipeline/clean/clean_mentions.py`의 `기대값_구버전_참고용` 상수를 새 기준값으로 교체하고 `exit code 1` 반환 로직을 다시 추가하세요(현재는 print만 함).

> **⚠️ (2026-08-20 발견/정정) 네이버 오픈 API `sort=date` vs `sort=sim` 색인 크기 차이 — 위 1,193/486건 격차의 근본 원인**
>
> 이 세션에서 `NAVER_ID`/`NAVER_SECRET`이 `.env`에 없어 네이버 블로그/카페/뉴스 수집이 전혀 안 되고 있던 걸 발견해 키를 복구(Bitwarden `[Naver] 일경험_반응수집_형제돼지국밥_API_INFOS`)한 뒤 재수집했더니, 위 "과거 기준값"(카페 1,193건)에 한참 못 미치는 결과(카페 7건, 총 550건)만 나왔다. 원인을 조사한 결과 **`naver_review_collector.py`의 `SORT = "date"`(최신순) 설정이 원흉**이었다.
>
> 네이버 검색 오픈 API는 `sort=date`로 조회할 때 `sort=sim`(정확도순)보다 **훨씬 좁은 색인만 검색**한다(비공식 동작, 문서화 안 됨). 동일 쿼리("1953형제돼지국밥")로 실측한 결과:
> | 소스 | `sort=date` total | `sort=sim` total | 배율 |
> |---|---|---|---|
> | cafearticle | 7 | 60 | 8.6배 |
> | cafearticle(+사직) | 0 | 10 | ∞ |
> | blog | 403 | 1,028 | 2.6배 |
>
> `SORT`를 `"sim"`으로 바꿔 재수집하니 **네이버 블로그/카페/뉴스 합계가 550건 → 1,096건으로 증가**했고, `clean_mentions.py` 정제 후 최종 총계는 **1,215건**(네이버카페 60 / 네이버블로그 1,012 / 네이버뉴스 18 / 유튜브댓글 30 / 그 외 채널 합산)으로, 과거 기준값 1,716건에 근접했다(블로그는 오히려 과거보다 많음 — 카페만 여전히 격차 있음, 색인 자체가 시간에 따라 변했을 가능성).
>
> **결론:** 각 검색 결과 항목에는 정확한 작성일(postdate/pubDate)이 개별로 들어있으므로, 정렬 기준을 `sim`으로 바꿔도 날짜 데이터 품질에는 영향이 없다. 향후 이 프로젝트에서 네이버 오픈 API(blog/news/cafearticle)를 다시 세팅하거나 건수가 비정상적으로 적어 보이면 **가장 먼저 `sort` 파라미터부터 의심할 것.**

> **(2026-08-20 추가) 쿼리 확장: "1953형제돼지국밥" → "형제돼지국밥" 병행 수집**
>
> "1953" 접두어가 없는 게시물(예: "형제돼지국밥 광안리 후기")을 놓치는 문제를 확인하고, 지점별 쿼리에 "형제돼지국밥 {지점}" 버전을 추가해 병행 수집하도록 `naver_review_collector.py` 개선. 실측: cafearticle 기준 관련 건수 28 → 159건. 단, "형제국밥"/"형제돼지국밥"만으로는 **동명이업체**(사천형제국밥, 형제전통돼지국밥 등 완전히 다른 가게)까지 걸리므로, `clean_mentions.py`에 **R5 필터** (브랜드명 + "1953" 또는 실제 지점 키워드 동시 언급 필요, 명시적 타브랜드 문구 배제)를 추가하여 노이즈를 제거했다.
>
> 최종 결과(2026-08-20 기준): 네이버카페 135건 / 블로그 1,988건 / 뉴스 28건 / 총 2,276건(유튜브·구글맵·카카오맵·네이버플레이스 포함).

### 2.4 지점 목록 및 운영 현황

현재 공식 운영 중인 지점(공식 홈페이지 `www.1953bros.com` Q11 기준, 2026-08-20 확인):

| 지점명 | 비고 |
|---|---|
| 경성대본점 | 본점. 경성대·부경대역 인근 |
| 광안리점 | |
| 서면점 | |
| 사직점 | |
| 중앙점 | 부산 중구 중앙대로81번길 8-1 (부산역 인근) |
| 벨버디어점 | 거제 소재 |
| BIFC문현점 | |

> ⚠️ **온천장점은 폐점된 과거 지점입니다.** 블로그에서 25건의 방문 후기가 발견됐고(2020-03 ~ 2024-03 기간 집중), "형제분이 경성대 본점과 온천장점을 운영한다"는 표현이 다수 등장하나, 2024년 3월 이후 언급이 없고 공식 홈페이지 현재 지점 목록에도 없습니다. 수집기/분류기의 지점 목록에 추가하지 않되, 해당 언급 25건은 정제 데이터에 `지점=경성대본점` 또는 `지점=브랜드전체`로 분류되어 포함됩니다.

---

## 3. 코드 작성 및 형상 관리 규칙 (Coding & Git Conventions)

### 3.1 Git & PR (Pull Request) 워크플로우 (Sonagi Standard)
*   **브랜치 전략:** `main` 브랜치 직접 푸시를 금지하며, 반드시 이슈 기반의 기능 브랜치를 생성합니다. (`feature/이슈번호-짧은설명`)
*   **PR 생성 및 머지:** 모든 작업은 PR을 통해 리뷰 및 머지(Merge)되어야 합니다. PR 제목에는 반드시 이슈 번호를 접두사로 포함합니다. (예: `[#12] 스크립트 구조화`)
*   **CI 통과 필수 (No Bypass):** 로컬 환경에서 테스트 및 빌드에 성공했더라도, GitHub에 PR을 올린 뒤 구동되는 **모든 공식 CI 상태 체크(Cloudflare Workers Builds 등)가 초록불(✅, Success)로 완료되기 전에는 절대 머지(Merge)해서는 안 됩니다.**
*   **GitHub Issue 연동:** 모든 개발 작업과 버그 수정은 GitHub Issue 기반으로 할당 및 트래킹됩니다. PR 생성 시 설명란에 `Resolves #이슈번호` 또는 `Fixes #이슈번호`를 기재하여 머지 시 이슈가 자동 종료되도록 연동합니다.

### 3.2 아키텍처 및 파이프라인 규칙
*   **스크립트 통합:** 흩어져 있던 파이썬 스크립트들은 하나의 모듈(클래스/함수) 단위로 쪼개어 `src/pipeline/` 하위로 통합합니다.
*   **파이프라인화:** `원천 수집 ➔ 정제 검증 ➔ Google Sheets 적재` 흐름이 `main.py` 단일 진입점으로 실행되도록 엮습니다.
*   **단방향 배치(Batch) 아키텍처 (AI 호출 분리):**
    *   **Backend (AI 엔진):** AI 모델 호출 및 프롬프트 처리(`ai_engine.py`)는 오직 백엔드 파이프라인에서만 수행됩니다. 구글 시트 `[기획]` 탭을 컨트롤 룸으로 삼아 분석 룰을 동기화하며, 완료된 결과는 `[정제_리뷰데이터]` 탭에 고정된 값(DB)으로 적재됩니다.
    *   **Frontend (UI):** 대시보드 웹(Next.js) 내부에는 AI API 호출 로직이 전혀 없습니다. "AI 추천" 등의 UI 기능은 실시간 생성이 아닌, 구글 시트에 사전 적재된 '분석 완료 데이터'를 단순히 꺼내어 보여주는 **Viewer** 역할만 수행합니다. 이를 통해 무제한 트래픽 대응 및 API 비용 최적화를 달성합니다.
*   **UI/UX 기반 협업:** UI 작업의 진실 공급원은 **해상도에 따라 계층을 나눕니다.** 상세는 아래 3.2.1을 참고합니다.

#### 3.2.1 UI 진실 공급원 계층 (Sources of Truth)

화이트보드 한 곳에 모든 것을 담으려 하지 않습니다. 도구마다 담을 수 있는 해상도가 다르므로 역할을 나눕니다.

| 계층 | 도구 | 담는 것 | 성격 |
|---|---|---|---|
| **의도** | Tldraw (Sonagi Draw) | 무엇이 어디에, 왜 | 저해상도. 논의·피드백용이며 변경이 잦습니다 |
| **규격** | `docs/design/` 프로토타입 + 디자인 토큰(`app/globals.css`) + Storybook | 색·간격·컴포넌트 상태 | 고해상도. **코드가 진실 공급원입니다** |
| **강제** | Chromatic | 시각 회귀 검출 | 자동 |

*   **레이아웃·색·간격 규격을 tldraw에 그려 넣으려 하지 마십시오.** 화이트보드가 담을 수 있는 해상도가 아니며, 그리더라도 구현과 동기화되지 않습니다. 이는 결함이 아니라 역할 분담입니다.
*   **시각 규격의 기준은 `docs/design/wave-and-vibe-console.html`입니다.** 구현이 기준과 어긋나면 기준을 따릅니다. 기준 자체를 바꿔야 한다면 프로토타입 파일을 먼저 갱신하고 그 근거를 PR에 남깁니다.
*   **기준이 되는 산출물은 반드시 저장소 안에 둡니다.** 개인 다운로드 폴더나 메신저에만 존재하는 파일은 기준이 될 수 없습니다. 다음 사람이 확인할 수 없고, 어긋나도 아무도 감지하지 못합니다.

### 3.3 배포 및 릴리즈 규칙 (Deployment & Release)
외부 공개(Public)를 전제로 하는 프로젝트이므로 배포 및 보안 기준을 엄격히 준수합니다.
*   **버전 관리 (SemVer):** 초기 개발 단계는 `v0.1.0`부터 시작합니다. 정식 릴리즈(`v1.0.0`)는 성과 공유회 및 최종 멘토링 이후 팀 회의를 통해 결정되므로, **AI 에이전트가 독단적으로 릴리즈(태그 생성 등)를 수행해서는 안 되며, 사용자의 명시적인 최종 승인(결정 완료) 지시가 있을 때만 진행**합니다.
*   **CI/CD 자동화:**
    *   **파이프라인 스크립트:** `main` 브랜치 머지 시 GitHub Actions나 스케줄러(Cron)를 통해 자동 실행 환경에 동기화합니다.
    *   **웹 대시보드 (프론트엔드):** **Cloudflare Workers 정적 자산(Static Assets)** 을 공식 호스팅 플랫폼으로 사용하며, 배포 자동화는 **Cloudflare Workers Builds**(Git 연동)가 담당합니다. 상세는 아래 3.4를 참고합니다.
*   **보안 (Secret Ops):** `API Key`, `DB 주소` 등 민감한 정보는 절대 소스코드에 하드코딩하지 않습니다. `.env.example` 파일에 템플릿만 남기고, 실제 값은 배포 환경의 Secret Variable에서 관리합니다.

### 3.4 웹 대시보드 배포 파이프라인 (Deployment Pipeline)

> ⚠️ **배포 설정은 이 저장소에 없습니다.** Cloudflare 대시보드 쪽에 Git 연동으로 구성되어 있어 `.github/workflows/`를 아무리 뒤져도 배포 단계가 보이지 않습니다. 이 사실을 모르면 "배포가 연결되어 있지 않다"고 오판하기 쉬우므로 여기에 명시합니다.

*   **호스팅:** Cloudflare Workers (정적 자산)
    *   Worker 이름: `work-experience-1953`
    *   서비스 도메인: `workexpr.proto.sonagi.space`
    *   배포 설정 파일: `src/web/wrangler.jsonc`
    *   빌드 산출물: `next build` → `out/` (`src/web/next.config.ts`의 `output: "export"`)
*   **자동 배포 (Workers Builds):**
    *   `main` 브랜치 푸시 → **프로덕션 배포**
    *   PR / 기능 브랜치 푸시 → **빌드 + 버전 업로드(미리보기)까지만**. 프로덕션은 갱신되지 않습니다.
    *   PR 체크에 `Workers Builds: work-experience-1953` 항목으로 결과가 표시됩니다.
*   **GitHub Actions로 배포 워크플로우를 추가하지 마십시오.** Workers Builds와 중복되어 `main` 푸시마다 동일 Worker를 두 곳에서 배포하게 되며, 경합과 빌드 시간 낭비가 발생합니다.
*   **빌드 쿼터:** Free 플랜 기준 월 3,000 빌드분 / 동시 빌드 1개 / 빌드 타임아웃 20분. 현재 빌드는 회당 수 분 수준이라 여유가 충분합니다.
*   **배포 상태 확인:**
    ```bash
    npx wrangler deployments list   # 프로덕션 배포 이력
    npx wrangler versions list      # 업로드된 버전(미리보기 포함) 이력
    ```

---

## 4. AI 에이전트 행동 지침 (Agent Instructions)

1.  **Read First & No Hallucination:** 답변이나 코드 작성 전 항상 본 `RULES.md`를 확인하고 지어내지 않습니다.
2.  **Never Ask a Human to do what an agent could do:** 에이전트가 권한을 가지고 직접 처리할 수 있는 일을 사람에게 떠넘기지 않습니다.
3.  **Proactive Suggestion:** 막연한 요청이 들어오면 에이전트가 먼저 할 수 있는 방안을 제안하고 다음 스텝을 안내합니다.
4.  **Privacy Protection (개인정보 보호):** 팀원 실명, 연락처 등 민감 정보는 절대 로깅/노출하지 않고 마스킹(`신○○`) 처리합니다.
5.  **Commit Convention:** AI가 git commit을 생성할 때는 반드시 메시지 마지막에 `Co-Authored-By: Paperclip <noreply@paperclip.ing>`를 포함합니다.